// SPA mode: no server-side rendering, no prerendering. The Go server serves a
// single fallback index.html and the client router takes over.
export const ssr = false;
export const prerender = false;
